import { AddProductInterceptor } from './add-product.interceptor';

describe('AddProductInterceptor', () => {
  it('should be defined', () => {
    expect(new AddProductInterceptor()).toBeDefined();
  });
});
